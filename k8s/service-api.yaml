# Scalability artifact only — not on the demo path. See k8s/README.md.
apiVersion: v1
kind: Service
metadata:
  name: prahari-api
spec:
  selector:
    app: prahari-api
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
